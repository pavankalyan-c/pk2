while true:
	number1=int(input("Enter Number"))
	number2=number1+1
	multipliers=list()
	for j in range(number1,o,-1)
	multipliers.append(j)
	#print(j)
	import operator
	import functools
	result=functools.reduce(operate.mul,multipliers,1)
	print(result)
